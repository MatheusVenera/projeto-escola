import styled from "styled-components";
import { primaryColor } from '../../config/colors'

export const Container = styled.div`
    margin-top: 20px;
`
export const ImageAluno = styled.img`
    width: 32px;
    height: 32px;
    border-radius: 50%;
`

export const ActionsButtons = styled.div`
    color: ${primaryColor}
`